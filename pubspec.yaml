name: doordash_drive_api
description: A client for the Doordash Drive REST API
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  mocktail: ^0.3.0
  retrofit_generator: ^8.1.0
  test: ^1.19.2
  very_good_analysis: ^5.0.0
dependencies:
  equatable: ^2.0.5
  http: ^1.1.2
  json_annotation: ^4.8.1
  retrofit: ^4.0.1
